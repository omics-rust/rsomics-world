## ampliconstats-st-reverify — 2026-05-23T16:04:45Z

- ours: `rsomics-bam-ampliconstats -t 1 /Volumes/KIOXIA/Documents/rsomics-world/crates/tools/formats/rsomics-bam-ampliconstats/tests/fixtures/primers_large.bed FIX <fixture>` @ 90fe43c9+dirty
- upstream: `samtools ampliconstats /Volumes/KIOXIA/Documents/rsomics-world/crates/tools/formats/rsomics-bam-ampliconstats/tests/fixtures/primers_large.bed FIX <fixture>` — version: samtools 1.23.1
- machine: Darwin arm64 | Apple M2 | 8 cores
- fixture: crates/tools/formats/rsomics-bam-ampliconstats/tests/fixtures/amplicons_large.bam — 131080101 bytes — sha256 022f490f2e9a86bed234706efe089a540386d61b84bd357296b99c2651245f88
- hyperfine: warmup 3, min-runs 10, shell=none

| side | mean (s) | σ | min | max |
|---|---|---|---|---|
| ours | 0.5873 | 0.0160 | 0.5738 | 0.6267 |
| upstream | 0.6295 | 0.0123 | 0.6113 | 0.6529 |

**ratio (upstream/ours): 1.0718× → PASS** (contract: strictly > 1.0×)


## vcf-fixref-flip-t1 — 2026-05-23T16:30:16Z

- ours: `rsomics-vcf-fixref <vcf> -f <ref.fa> -m flip -o OUT` @ eb5e6fee+dirty v0.1.0
- upstream: `bcftools +fixref <vcf> -o OUT -- -f <ref.fa> -m flip` — version: bcftools 1.23.1
- machine: Darwin arm64 | Apple M2 | 8 cores
- fixture VCF: crates/tools/formats/rsomics-vcf-fixref/tests/fixtures/fixref_bench.vcf — 36217365 bytes — sha256 f46c4da9b2311c3cd6593970b1bf5fc5acf7ac505666b16d7b3bb4ec0822f99b
- fixture FASTA: crates/tools/formats/rsomics-vcf-fixref/tests/fixtures/fixref_bench.fa (244M, 24 chroms × 10Mbp ACGT periodic)
- hyperfine: warmup=3, min-runs=10, shell=sh (cache-warm run after 3 warmups)
- note: variance in cold-cache run is high (0.78–3.1s range); extended run with warm FS cache used

| side | mean (s) | σ | min | max |
|---|---|---|---|---|
| ours | 0.7893 | 0.0188 | 0.7687 | 0.8168 |
| upstream | 3.105 | 0.089 | 3.028 | 3.292 |

**ratio (upstream/ours): 3.93× → PASS** (contract: strictly > 1.0×)

Hot-path analysis: bcftools system time = 2.47s vs ours = 0.33s — bcftools uses htslib's
`faidx_fetch_seq()` which malloc+memcpy-allocs a string per site; our tool does a
single-byte seek+read_exact per site with no heap allocation.


## vcf-fixref-flip-t1-with-fixref-annotation — 2026-05-24

Context: re-measured after adding byte-exact `FIXREF=<action>` INFO annotation (the false-green
compat fix). The annotation adds a `format!` + string replace per record.

- ours: `rsomics-vcf-fixref <vcf> -f <ref.fa> -m flip -o OUT` @ eb5e6fee+dirty v0.1.0
- upstream: `bcftools +fixref <vcf> -o OUT -- -f <ref.fa> -m flip` — version: bcftools 1.23.1
- machine: Darwin arm64 | Apple M2 (mini_m2), macOS 26.5
- fixture VCF: tests/fixtures/fixref_bench.vcf — 36 MB, 500 028 records, sha256 f46c4da9b2311c3cd6593970b1bf5fc5acf7ac505666b16d7b3bb4ec0822f99b
- fixture FASTA: tests/fixtures/fixref_bench.fa — 244 MB
- hyperfine: warmup 2, runs 5, shell=sh

| side | mean (s) | σ | min | max |
|---|---|---|---|---|
| ours | 0.985 | 0.027 | 0.960 | 1.027 |
| upstream | 3.094 | 0.025 | 3.069 | 3.133 |

**ratio (upstream/ours): 3.14× → PASS** (contract: strictly > 1.0×)

Delta vs pre-annotation: 0.789 s → 0.985 s (+0.196 s, +25%). Annotation adds one `format!`
call and one string rebuild per record; the 25% overhead on 500k records is expected and
acceptable — still 3.14× ahead of bcftools.

## fixref-st-reverify — 2026-05-23T16:50:52Z

- ours: `rsomics-vcf-fixref -t 1 -f /Volumes/KIOXIA/Documents/rsomics-world/crates/tools/formats/rsomics-vcf-fixref/tests/fixtures/fixref_bench.fa -m flip FIX <fixture>` @ eb5e6fee+dirty
- upstream: `bcftools +fixref FIX -- -f /Volumes/KIOXIA/Documents/rsomics-world/crates/tools/formats/rsomics-vcf-fixref/tests/fixtures/fixref_bench.fa -m flip <fixture>` — version: bcftools 1.23.1
- machine: Darwin arm64 | Apple M2 | 8 cores
- fixture: crates/tools/formats/rsomics-vcf-fixref/tests/fixtures/fixref_bench.vcf — 36217365 bytes — sha256 f46c4da9b2311c3cd6593970b1bf5fc5acf7ac505666b16d7b3bb4ec0822f99b
- hyperfine: warmup 3, min-runs 10, shell=none

| side | mean (s) | σ | min | max |
|---|---|---|---|---|
| ours | 48.8326 | 1.5596 | 46.7662 | 52.1393 |
| upstream | 3.0733 | 0.0227 | 3.0355 | 3.1075 |

**ratio (upstream/ours): 0.0629× → FAIL** (contract: strictly > 1.0×)

## fixref-st-hashcache — 2026-05-23T17:19:02Z

- ours: `rsomics-vcf-fixref -f /Volumes/KIOXIA/Documents/rsomics-world/crates/tools/formats/rsomics-vcf-fixref/tests/fixtures/fixref_bench.fa -m flip FIX <fixture>` @ eb5e6fee+dirty
- upstream: `bcftools +fixref FIX -- -f /Volumes/KIOXIA/Documents/rsomics-world/crates/tools/formats/rsomics-vcf-fixref/tests/fixtures/fixref_bench.fa -m flip <fixture>` — version: bcftools 1.23.1
- machine: Darwin arm64 | Apple M2 | 8 cores
- fixture: crates/tools/formats/rsomics-vcf-fixref/tests/fixtures/fixref_bench.vcf — 36217365 bytes — sha256 f46c4da9b2311c3cd6593970b1bf5fc5acf7ac505666b16d7b3bb4ec0822f99b
- hyperfine: warmup 3, min-runs 10, shell=none

| side | mean (s) | σ | min | max |
|---|---|---|---|---|
| ours | 0.9009 | 0.0207 | 0.8777 | 0.9419 |
| upstream | 3.1115 | 0.0280 | 3.0691 | 3.1629 |

**ratio (upstream/ours): 3.4539× → PASS** (contract: strictly > 1.0×)


## bam-signal-st — 2026-05-24T01:45:00Z

- ours: `rsomics-bam-signal -t 1 -o OUT FIX` @ 7a306224
- upstream: `bamCoverage -b FIX -o OUT.bedgraph --outFileFormat bedgraph --binSize 50 -p 1` — version: bamCoverage 3.5.6
- machine: Darwin arm64 | Apple M2 | 8 cores
- fixture: bench.bam (markdup_1m_cs synthetic WGS, coord-sorted, ~1M reads) — 62369993 bytes — sha256 e202f792df45409d2e6498ff80319407d09a32f6fe6a0c4431d2ffdb563e63b6
- hyperfine: warmup 2, min-runs 7, default shell

| side | mean (s) | σ | min | max |
|---|---|---|---|---|
| ours | 0.2567 | 0.0051 | 0.2499 | 0.2681 |
| upstream | 3.9862 | 0.0436 | 3.9433 | 4.0672 |

**ratio (upstream/ours): 15.5312× → PASS** (contract: strictly > 1.0×)

Note: deeptools is single-process Python with pysam/numpy overhead. Our Rust implementation uses rsomics-bamio raw-record path with libdeflate BGZF, flat Vec<u32> bin arrays, no per-record allocation, and a single scan pass. All bins (including zero) emitted; adjacent equal-value bins merged. Output byte-exact vs deeptools 3.5.6 on 62MB fixture.


## rsomics-bam-signal (deeptools bamCoverage) — 2026-05-24
- ours `-t 1 <bam> -o OUT` vs `bamCoverage -b <bam> -o OUT --outFileFormat bedgraph --binSize 50 -p 1`
- **-t1 fair (thread-matched, warm hyperfine -r7): 14.86 ± 0.81x faster** (ours ~0.27s vs deeptools ~4.0s)
- upstream: deeptools bamCoverage 3.5.6 (pip, macOS arm64 Apple M2)
- fixture: tests/fixtures/bench.bam — 62MB coord-sorted, ~1M reads (gitignored)
- compat: BYTE-EXACT bedGraph vs deeptools 3.5.6 (golden binSize 50+100 + 62MB bench)
- NOTE: scripts/perfgate.sh can't be used here — it symlinks only the BAM, dropping the .bai that deeptools (and indexed readers) require; measured via direct hyperfine instead. (perfgate .bai-pair handling = a harness follow-up.)


## vcf-setgt-t-q — 2026-05-24

- ours: `rsomics-vcf-setgt --target q -i 'FMT/DP<5' -n . -o OUT` @ rsomics-vcf-setgt v0.2.0
- upstream: `bcftools +setGT -o OUT -- -t q -i 'FMT/DP<5' -n .` — version: bcftools 1.23.1
- machine: Darwin arm64 | Apple M2 | 8 cores (mini_m2)
- fixture: /tmp/setgt-bench.vcf — 93.9 MB — 100,000 records × 100 samples — GT:DP:GQ (seed 42, random.randint)
- hyperfine: warmup 1, runs 5

| side | mean (s) | σ |
|---|---|---|
| ours (-t q) | 0.776 | 0.090 |
| bcftools (-t q) | 0.936 | 0.028 |

**ratio (upstream/ours): 1.21× → PASS** (contract: strictly > 1.0×)

### Simple mode (-t a -n .) — no expression eval

| side | mean (s) | σ |
|---|---|---|
| ours (-t a) | 0.362 | 0.043 |
| bcftools (-t a) | 0.799 | 0.030 |

**ratio (upstream/ours): 2.21× → PASS**


## rsomics-fragment-size (PE insert-size distribution) — 2026-05-24
- correctness AUTHORITY: VALUE-EXACT vs `samtools stats -m 1.0` IS-lines (0 differing sizes on golden 2000-pair + 1.5M-pair bench; counts paired+proper-pair+mapped+read1 |TLEN|, matching samtools' inward-FR-pair rule; samtools default -m 0.99 trims top 1%, -m 1.0 = no-trim match). cross-check: value-exact vs picard CollectInsertSizeMetrics histogram too.
- perfgate (fair same-op, full-scan): ours -t1 vs `picard CollectInsertSizeMetrics` on 62MB/1.2M-pair PE BAM (mac M2): subagent hyperfine 289.6ms vs 9.616s = 33.2x; parent re-verify ~0.27s vs 10.5s ≈ 39x. Overwhelming (picard = JVM+R full-scan, genuine compute). PASS.
- (samtools stats -m 1.0 = 2.74s but does MANY stats beyond IS → not the fair same-op oracle; picard is.)

## rsomics-methyldackel (MethylDackel extract) — 2026-05-24
- BYTE-EXACT vs real MethylDackel 0.6.1 (BUILT FROM SOURCE: htslib brew + libBigWig -DNOCURL) — golden authentic (not circular), data lines identical on golden + 74M output on calmd_30x.bam (2.5M CpG lines, diff=0).
- -t1 perfgate: ours vs real MethylDackel extract single-thread (calmd_30x.bam 92MB/4M reads, mac M2, hyperfine 6 runs): 1.59x (re-verified; subagent fix reported 1.58x). PASS.
- FIX history: 1st build claimed 3.8x but real -t1 was 0.66x (6th overclaim; allocation/page-fault storm — buffered ALL reads → 2.2GB RSS, 3.09s system). Fixed via streaming overlap-window (settle reads when ref-end passes cursor, scatter CpG calls, drop; split-borrow mate-pair, zero per-pair clones): RSS 2.2GB→116MB, system 3.09s→0.10s, -t1 0.66x→1.59x.

## rsomics-bam-fingerprint (deeptools plotFingerprint) — 2026-05-24
- BYTE-EXACT vs deeptools plotFingerprint 3.5.6 --outRawCounts: 216/216 config combos (4 BAMs × 9 bin-sizes × 6 sample-counts) + the real 62MB BAM at full defaults (498540 bins). Matched deterministic stepSize sampling + both deeptools code paths (stepSize!=binSize single-bin clamp vs stepSize==binSize tiled over-count quirk) + CIGAR get_blocks (M/=/X split by I/D/N) + skip-unmapped filter.
- perfgate -t1 vs plotFingerprint -p1 (62MB/1.2M-read indexed BAM, mac M2): DEFAULT (bs500 n500000) ours 0.52s vs deeptools ~1357s (deeptools default = 500k separate index fetch() calls, pathological); matched n1000 = 8.26x (subagent). Win overwhelming at every matched config. PASS.
- scoped: PNG/SVG plot (we emit the data table, like bam-signal→bedGraph), --JSDsample JS-distance/CHANCE/Synthetic cols.

## rsomics-coverage-core (Layer-A) + rsomics-bam-compare (deeptools bamCompare) — 2026-05-24
- REFACTOR GUARD: bam-signal (published 0.1.0→0.1.1) migrated to consume the new rsomics-coverage-core Layer-A binned-coverage primitive — STILL BYTE-EXACT vs deeptools bamCoverage (compat + 62MB/180449-line check, 278ms -t1, no regression). Extraction behavior-preserving. no-cp honored.
- bam-compare: BYTE-EXACT vs deeptools bamCompare 3.5.6 — all 8 ops (log2/ratio/reciprocal_ratio/subtract/add/mean/first/second) + binSize 50/100 + pseudocount, golden + 62MB self-compare (log2) + subagent's 62MB+115MB/197853-line pair. Default readCount scaling (min(m1,m2)/mi) + log2 + pseudocount[1,1] per bamCompare.py.
- perfgate -t1 vs bamCompare -p1: re-verified 32.26x (62MB self-compare bs50, hyperfine); subagent 15.54x (62MB+115MB pair). PASS overwhelmingly (deeptools single-thread Python).
- scoped: SES scaling, --normalizeUsing, extend/center, smoothLength, region/blacklist, bigWig.

## rsomics-multibam-summary (deeptools multiBamSummary) — 2026-05-24
- Built on rsomics-coverage-core 0.1.0 (compute_coverage per BAM -> assembled matrix). 3rd consumer of the Layer-A lib.
- VALUE-EXACT vs deeptools multiBamSummary 3.5.6 --outRawCounts: bins mode (10000 bins, big_a+big_b ~chr1 fixtures, header + ALL rows byte-identical) AND BED-file mode (500 regions). -t1 output identical to default-thread.
- perfgate FAIR -t1: ours -t1 473.6ms +/-3.6 vs deeptools bins -p1 9.600s +/-0.098 = 20.27x +/-0.26 (hyperfine r4 warmup1, Mac14,3 arm64). NOTE default-thread ours = 67.9x but that is multi-vs-single (unfair); 20.27x is the honest thread-for-thread number. PASS >1.0x.
- modes: bins + BED-file feature-complete; numpy .npz output scoped (--outRawCounts is the value oracle). re-verified by parent (killed subagent's claims not trusted).

## compute-matrix — 2026-05-24

deeptools `computeMatrix` port (bigWig signal → per-region score matrix). Own
pure-Rust bigWig reader (`src/bigwig.rs`, flate2 zlib-rs inflation) — bigtools
NOT adopted: it pins `libdeflater 0.13` whose C-FFI `libdeflate-sys`
(`links="libdeflate"`) collides at link time with the workspace's
`rsomics-fqgz` (`libdeflater 1.x`).

- ours: `rsomics-compute-matrix {reference-point|scale-regions} -S bench.bw -R bench.bed -o OUT.gz ... -t 1 -q` @ v0.1.0
- upstream: `computeMatrix {reference-point|scale-regions} -S bench.bw -R bench.bed -o OUT.gz ... -p 1 -q` — version: deeptools 3.5.6 (miniforge)
- machine: Darwin arm64 | Apple M2 (mini_m2) | 8 cores, macOS 26.5
- fixture bigWig: tests/fixtures/bench.bw — 258810 bytes — sha256 b2f010877e34884a746ddc862c1d60010b1d8e811a179702cd4f6e298d3d64ea (3 chroms, peaky 25-bp bigWig from synthetic 86k-read BAM)
- fixture BED: tests/fixtures/bench.bed — 2100 BED6 regions, mixed strand — sha256 ce062be10864f39aa18304c06671fd0370ea545d517876e2604a5383380f9918
- hyperfine: warmup 1, min-runs 5

VALUE-EXACT vs deeptools 3.5.6: ours -t1 data rows byte-identical to deeptools -p1
on the 2100-region fixture (reference-point TSS/TES/center + scale-regions), and
header JSON byte-identical save `proc number` (a runtime knob). Both single-thread.

reference-point (TSS, b2000 a2000 bs50):

| side | mean (s) | σ | min | max |
|---|---|---|---|---|
| ours -t1 | 0.3351 | 0.0032 | 0.3303 | 0.3400 |
| deeptools -p1 | 3.416 | 0.041 | 3.368 | 3.470 |

**ratio (upstream/ours): 10.19× → PASS** (contract: strictly > 1.0×)

scale-regions (m2000 b1000 a1000 bs50):

| side | mean (s) | σ | min | max |
|---|---|---|---|---|
| ours -t1 | 0.4728 | 0.0047 | 0.4661 | 0.4789 |
| deeptools -p1 | 3.309 | 0.035 | 3.270 | 3.360 |

**ratio (upstream/ours): 7.00× → PASS**

Peak RSS (/usr/bin/time -l, reference-point): ours 5.5 MB vs deeptools 70.2 MB
(~12.7× lower). deeptools loads per-base into numpy float arrays; ours streams
per-flank.

modes: reference-point feature-complete (TSS/TES/center, both strands, asym
flanks, off-chrom NaN pad, all 6 bin stats, missing-data-as-zero, scale, min/max
threshold, skip-zeros); scale-regions feature-complete (scaled body + flanks,
both strands, short-body all-NaN case). Scoped: multi-sample/multi-group,
#-grouped BED, GTF, unscaled 5'/3', nanAfterEnd, blacklist, sortRegions!=keep.

A real bug (numpy linspace float-rounding at bin boundaries: must compute
`step=total/nbins` then `idx*step`, not `idx/nbins*total`) was caught only by the
large fixture — small fixture was a false-green.

## rsomics-compute-matrix (deeptools computeMatrix) — 2026-05-24 [PARENT RE-VERIFIED]
- Self-built pure-Rust BBI/bigWig reader (src/bigwig.rs, flate2 zlib-rs). bigtools UNUSABLE: its libdeflater 0.13 (links="libdeflate") collides with workspace rsomics-fqgz/fastq-* libdeflater 1.x (single-links rule = cargo hard error). Quadrant (1) pure-Rust. format from bigtools source (MIT) + Kent BBI spec.
- VALUE+HEADER EXACT vs deeptools 3.5.6 (parent-verified LIVE on 2100-region bench fixture, both modes): reference-point (TSS/TES/center) AND scale-regions — @-JSON header + ALL matrix rows byte-identical (excl runtime "proc number"). -t1 output identical to default.
- FAIR perfgate -t1 (parent re-verified; ours -t1 User~=wall = genuine single-thread): reference-point ours 340.6ms vs deeptools -p1 3.424s = 10.05x +/-0.27; scale-regions ours 469.6ms vs 3.295s = 7.02x +/-0.06. PASS. subagent reported 10.19x/7.00x — matches. Peak RSS ~12.7x lower.
- bug the small fixture hid (caught by subagent on large): numpy linspace bin partition = step=total/nbins then idx*step (float-rounding at boundaries). modes feature-complete; multi-sample/group, GTF, blacklist, clustering scoped.

## rsomics-bbi (Layer-A) + rsomics-bigwig-compare (deeptools bigwigCompare) — 2026-05-24 [PARENT RE-VERIFIED]
- GUARD: compute-matrix (0.1.0->0.1.1) migrated to consume new Layer-A rsomics-bbi (extracted self-built BBI reader) — STILL value-exact vs deeptools (all 19 tests incl live_diff_against_deeptools green). no-cp honored.
- rsomics-bbi 0.1.0: pure-Rust BBI reader (BigWig::open / chrom_len / chroms / values). flate2 zlib-rs. Quadrant (1).
- bigwig-compare: VALUE-EXACT vs deeptools bigwigCompare 3.5.6 (parent-verified live): log2 (149031 lines) + subtract (148675 lines) on big pair byte-identical. all 8 ops, defaults match (log2/bs50/pseudocount[1,1]/missingDataAsZero). fixed misleading '-b1' help usage -> '-1/-2' (actual clap shorts).
- FAIR perfgate -t1 (ours single-thread, User~=wall): log2 ours 647ms vs deeptools -p1 16.892s = 26.11x; subtract 39.32x (subagent). PASS.

## rsomics-multibigwig-summary (deeptools multiBigwigSummary) + rsomics-bbi 0.1.1 zoom — 2026-05-24 [PARENT RE-VERIFIED]
- rsomics-bbi 0.1.0->0.1.1: +mean_stat; +zoom-level reading replicating libBigWig bwStats exact=0 (determine_zoom_level + zoom CIR-tree + getScalar fractional overlap + sumData/validCount, f64::mul_add FMA for 1-ULP match); +BATCHED zoom_items_for_chrom/full_items_for_chrom (one CIR-tree walk per chrom, O(items+bins) forward sweep). additive; published consumers (compute-matrix 0.1.1, bigwig-compare) unaffected — guard green.
- multibigwig-summary: VALUE/BYTE-EXACT vs deeptools multiBigwigSummary 3.5.6 (parent-verified): bins zoom path (bs 10000/5000) + full-res fallback (bs1000: no zoom level qualifies, min reduction 1008 > bs/2) + BED mode. deeptools uses pyBigWig stats(exact=False) zoom approx; ours replicates exactly.
- FAIR perfgate -t1 (parent re-verified): bs1000 ours 147.7ms vs deeptools -p1 8.201s = 55.58x; bs10000 ours 7.4ms vs 832.5ms = 112x. (3 rounds: r1 fast-exact byte-WRONG; r2 per-bin zoom byte-exact but 0.83x at bs1000 [fixture-size mirage 1.04x at bs10000]; r3 batched = byte-exact AND fast.)

## rsomics-bbi 0.1.2 (bigWig WRITER) + rsomics-bam-signal 0.1.2 (--out-file-format bigwig) — 2026-05-24 [PARENT RE-VERIFIED]
- rsomics-bbi 0.1.2: +write_bigwig (64B header / chrom B-tree / bedGraph-type-1 sections one-block-per-chrom / CIR-tree index / zoom-level reductions [128*bin_size base, x4, <=10 levels], flate2 zlib-rs). pure-Rust, bigtools still unusable.
- bam-signal 0.1.1->0.1.2: +-F/--out-file-format {bedgraph|bigwig}, default bigwig (matches deeptools bamCoverage). bedGraph output STILL byte-exact (regression guard verified).
- VALIDITY (KEY bar, parent-verified): deeptools multiBigwigSummary reads BOTH ours.bw + deeptools dt.bw → 200000 bins (62MB bench.bam, bs50): 0 mismatches. deeptools parses ours.bw without error → full-res + zoom data correct AS DEEPTOOLS READS IT.
- FAIR perfgate -t1: ours 359.9ms vs bamCoverage --outFileFormat bigwig -p1 4.202s = 11.67x. guard (compute-matrix/bigwig-compare/multibigwig-summary, 23 ok-blocks) green.
- follow-up: bam-compare --outFileFormat bigwig (deeptools bamCompare also defaults bigwig) not yet done.

## rsomics-bam-compare 0.1.1 (--out-file-format bigwig) — 2026-05-24 [PARENT RE-VERIFIED]
- +-F/--out-file-format {bedgraph|bigwig}, default bigwig (deeptools bamCompare default), via rsomics-bbi 0.1.2 write_bigwig. mirrors bam-signal 0.1.2.
- bedGraph BYTE-EXACT vs deeptools bamCompare bedgraph — verified on DIFFERENT subsamples (ba=50%/bb=30%, non-trivial scale factors [0.599,1.0]), not just self-compare; 10 ops compat tests pass. (confirms published 0.1.0 scaling is correct on different BAMs.)
- bigWig: deeptools reads ours.bw OK; value-exact to f32 precision — max|diff| 5.0e-6, mean 4.2e-7, ZERO diffs >1e-4. Residual = numpy-vs-Rust log2 transcendental-libm ULP (unavoidable cross-language; bedGraph's 6-decimal display masks it). NOT bit-identical (f32 ULP on ~all bins) but value-correct at deeptools' display precision. (Contrast bam-signal bigwig = 0 mismatches: coverage is exactly f32-representable; log2 is not.)
- FAIR perfgate -t1: ours 353.6ms vs bamCompare --outFileFormat bigwig -p1 3.767s = 10.65x (10Mbp ba/bb). write path fast (bigwig only +6% over bedgraph). subagent's 1.98x was a large-genome fixture (both slow), not a write inefficiency.

## rsomics-bamio 0.1.10 (work-stealing parallel BGZF writer) + rsomics-bam-calmd 0.1.0 — 2026-05-24 [PARENT RE-VERIFIED + L3]
- #78 FOUNDATION: new par_writer.rs WorkStealingBgzfWriter (additive; existing BatchBamWriter untouched, 13 other bam tools unaffected). Fixed ring N=workers*2+1 reusable (idata,cdata) slots = ZERO per-block alloc (vs noodles MultithreadedWriter's per-block oneshot channel); libdeflater level-6 deflate pool; ordered BTreeMap collector. Mirrors samtools bgzf_mt.
- calmd wired to it (file-output paths). CORRECTNESS (parent-verified): output BYTE-IDENTICAL across -t1/-t4/-t8; record content == samtools calmd (MD5 match, 1.2M records on bench.bam+synthetic chr1/chr2 ref).
- FAIR perfgate (parent re-verified, thread-for-thread, quiet mac M2, 62MB bench.bam): -t1 ours vs samtools -@1 = 1.61x; -t4 vs -@4 = 1.61x; -t8 vs -@8 = 1.34x. ALL >1.0x. Previously calmd -t4 = 0.86x (the #78 ceiling) → now 1.61x. (subagent reported 1.90/1.67/1.41 on a 115MB fixture; parent's quiet-machine numbers on 62MB.)
- L3 review: fresh-Opus concurrency audit = no deadlock (n_slots=2w+1 proven sufficient), no data race (per-slot Mutex = memory fence), no hang on error paths. One acceptable fail-loud item (worker panic poisons slot Mutex -> writer panic, not hang; .expect confirmed infallible). Codex unavailable this session (not logged in) -> Anthropic-only review.

## WS-writer rollout pilot (bamio 0.1.10 #78) — 2026-05-24 [PARENT RE-VERIFIED]
- KEY FINDING: the WS writer only helps tools whose UPSTREAM is HEAVY. Light-upstream tools tie (pure BGZF-write race vs samtools bgzf_mt = same algorithm, no compute advantage to leverage).
- addreplacerg 0.1.0->0.1.1: adopted WS writer (samtools addreplacerg is heavier than samtools reset → ours wins). FAIR re-verified: -t4 1.35x (was 1.16x), -t8 1.14x, -t1 1.28x. Byte-identical across -t1/-t4/-t8 AND vs samtools (real-tab -r; shell '\t' literal is a -r parsing artifact, both sides need a real tab). SHIP.
- reset: WS rollout REVERTED (stays 0.1.0). Subagent OVERCLAIMED 1.23x/1.17x; parent quiet-machine re-verify = -t4 1.06x (noise floor), -t8 ~0.97x (LOSES). reset's light upstream (samtools reset) → -t>=2 pure writer race = tie. reset stays DONE on -t1 1.19-1.30x raw-path win; -t>=2 tie is acceptable for a light passthrough op.
- fixmate (1.15x -t4) / markdup (1.17x) compute-bound + already pass; collate 3.78x (samtools temp-file disk approach) — none rolled.

## rsomics-derep (vsearch --derep_fulllength) — 2026-05-24 [PARENT RE-VERIFIED, 3 rounds] — first metagenomics tool
- BYTE-EXACT incl ORDER vs vsearch 2.31.0 (parent-verified: adversarial 120k-seq/12k-distinct heavy-ties + case+short+U+ties probes, diff empty).
- 3 re-verify rounds caught byte-exact bugs the subagent's all-uppercase >=32bp no-tie fixtures missed: (1) ours uppercased output, vsearch PRESERVES representative original case (U kept; normalize only as hash key); (2) missing default --minseqlength 32 + maxseqlength 50000; (3) tie-order — vsearch derep_compare_full = size DESC -> strcmp(RAW label incl ;size=, truncated at whitespace) -> seqno_first; ours stripped ;size= before compare.
- FAIR -t1 vs single-threaded vsearch: 1.49x (heavy-ties adv) .. 2.72x (200k bench). rayon parallel normalize. PASS.

## rsomics-fastx-sort (vsearch --sortbysize / --sortbylength) — 2026-05-24 [PARENT RE-VERIFIED] — 2nd metagenomics tool
- BYTE-EXACT both modes (--mode size|length) vs vsearch 2.31.0 (parent-verified: adversarial 100k heavy-ties/mixedcase/short<32/U + 400k synthetic, diff empty).
- comparators: sortbysize = size DESC -> strcmp(RAW label incl ;size=) -> seqno_first; sortbylength = length DESC -> size DESC -> strcmp(raw label). DEFAULT minseqlength=1 for SORT (NOT 32 = clust/derep/search), maxseqlength 50000. case+U preserved.
- FAIR -t1 vs single-threaded vsearch: sortbysize 1.57x, sortbylength 1.66x (400k synthetic). PASS.

## rsomics-rereplicate (vsearch --rereplicate) — 2026-05-24 [PARENT RE-VERIFIED] — 3rd metagenomics tool
- BYTE-EXACT (default + --sizeout) vs vsearch 2.31.0 (parent-verified: adversarial 20k size1-10/case/U/short/mid-;size=, diff empty both modes).
- vsearch --rereplicate: opt_xsize=true strips first ;size= token, abundance=1, default no size annotation; --sizeout appends ;size=1; NO minseqlength/maxseqlength (not in rereplicate's allowed-option set); fasta_width 80; case+U preserved.
- FAIR -t1 vs single-threaded vsearch: 2.54x (20k) .. 3.65x (50k). PASS.

## rsomics-derep-prefix (vsearch --derep_prefix) — 2026-05-24 [PARENT RE-VERIFIED] — 4th metagenomics tool
- BYTE-EXACT vs vsearch 2.31.0 (parent-verified: prefix-heavy adversarial 15097 recs [prefix groups + ties + case + U + short] + 74k, diff empty).
- prefix-merge: sort shortest-first, FNV-1A prefix hashes, representative = LONGEST seq in cluster, abundance summed; output size DESC -> strcmp(raw header) -> seqno_first; minseqlength 32 (derep family); case+U preserved.
- FAIR -t1 vs single-threaded vsearch: 1.28x (prefix-heavy) .. 1.38x (74k). PASS.

## rsomics-bam-strandedness (RSeQC infer_experiment) — 2026-05-24 [PARENT RE-VERIFIED] — first transcriptomics tool
- BYTE-EXACT vs RSeQC 5.0.4 (parent-verified: golden fwd_pe + independent 50k forward-stranded synthetic, diff empty). output: 2 leading \n + "This is PairEnd Data" + 3 "Fraction..." lines {:.4}. determinism via fixture < sample-size 200000 (all reads, no random sample).
- CLI --input/--refgene, --mapq long-only (avoids CommonFlags -q). rsomics-bamio 0.1.10 + interval tree for BED12.
- FAIR -t1 vs RSeQC Python: 30.36x. PASS.

## rsomics-bam-strandedness 0.1.1 — CI FIX (toolchain-dependent coitrees metadata) — 2026-05-24
- 0.1.0 (published, NOW YANKED) had `*node.metadata`: compiles on rust-1.95 (coitrees query metadata = &usize) but FAILS E0614 on CI/stable (metadata = usize). Toolchain-dependent coitrees 0.4.0 API quirk. Also a benches/strandedness.rs fmt diff.
- FIX 0.1.1: `*std::borrow::Borrow::<usize>::borrow(&node.metadata)` normalizes usize-or-&usize → compiles on both toolchains; fmt auto-fixed. byte-exact vs RSeQC unchanged. clippy --all-targets + fmt + tests clean locally.
- LESSON: re-verify clippy/fmt MYSELF (not just byte-exact+perf — I trusted the subagent's clippy/fmt claim); gate publish on CI GREEN (used `;` not `&&` → published a red-CI broken crate); watch toolchain-dependent dep-API types.

## rsomics-bam-read-dist (RSeQC read_distribution) — 2026-05-24 [PARENT RE-VERIFIED] — 2nd transcriptomics tool
- BYTE-EXACT vs RSeQC 5.0.4 (parent-verified: golden 73-read + 64k-read large, diff empty both). category precedence CDS>5'UTR>3'UTR>intron>TSS/TES bins; "%-30s%d" headers + "%-20s%-20d%-20d%-18.2f" rows + (count+1) denom. coitrees Borrow pattern (toolchain-robust).
- clippy/fmt/test/workspace-build clean (parent self-verified, per the publish-gate lesson). FAIR -t1 vs RSeQC Python: 22.56x. large_reads.bam golden = 349KB (OK).

## rsomics-bam-junctions (RSeQC junction_annotation) — 2026-05-24 [PARENT RE-VERIFIED] — 3rd transcriptomics tool
- BYTE-EXACT vs RSeQC 5.0.4 CLASSIFICATION (parent verified the meaningful output, not just total): golden covers all 3 classes — Known 4/Partial 2/Novel 1 Events + Known 3/Partial 2/Novel 1 Junctions, all match RSeQC stderr; 50k total matches. STRENGTHENED compat (subagent only asserted `total=` stdout; parent added the known/novel/partial stderr classification assert — caught a weak-compat gap).
- known/partial/novel per N-op vs BED12 intron-boundary sets; events=per-read, junctions=distinct; defaults -m 50/--mapq 30; coitrees Borrow pattern.
- clippy/fmt/test/workspace-build clean (self-verified). FAIR -t1 vs RSeQC Python: 22.2x (50k).

## rsomics-genebody-coverage (RSeQC geneBody_coverage) — 2026-05-24 [PARENT RE-VERIFIED] — 4th transcriptomics tool
- VALUE-EXACT (field-level f64 zero-tolerance, NOT byte) vs RSeQC 5.0.4: 100-bin 5'->3' coverage profile numerically identical (golden + 50k synthetic, all 100 values match). RSeQC emits a Python int/float HYBRID (2.0,2.0,2.0,4,5,...,7,7.0,6.0 — type-bleeding from 0.0-init dicts); ours emits consistent ints (numerically identical + cleaner). Deliberately NOT byte-matching Python's per-value int/float quirk (meaningless). compat asserts the full 100-value profile parsed as f64.
- banker's rounding (round-half-even) for percentile positions = Python round(); minus-strand reversed 5'->3'; >=100nt mRNA filter; coitrees Borrow pattern.
- clippy/fmt/test/workspace-build clean (self-verified). FAIR -t1 vs RSeQC Python: 14.6x (50k, wall>=80ms).

## rsomics-read-duplication (RSeQC read_duplication) — 2026-05-24 [PARENT RE-VERIFIED] — 5th transcriptomics tool
- BYTE-EXACT both .seq.DupRate.xls + .pos.DupRate.xls vs RSeQC 5.0.4 (parent-verified golden + 100k synthetic, diff empty both). position key = chrom:hit_st:exon-blocks (M emit+advance, D/N/S advance, I skip); filter skip unmapped(0x4)/qcfail(0x200) + mapq<thr (no secondary/dup filter). compat asserts BOTH tables (junctions weak-compat lesson applied by subagent).
- clippy/fmt/test/workspace-build clean (self-verified). FAIR -t1 vs RSeQC Python: 9.12x (100k).

## rsomics-read-gc (RSeQC read_GC) — 2026-05-24 [PARENT RE-VERIFIED] — 6th transcriptomics tool
- FIELD-EXACT (sorted) vs RSeQC 5.0.4 GC.xls (parent golden 7-bin + 500k synthetic). GC% = "%.2f" (G+C)/len*100 (N in denominator, not GC numerator); filter skip unmapped/qcfail + mapq<thr (no secondary). ours emits sorted-by-GC% (deterministic); RSeQC dict-insertion order; compat sorts both = field-level set match.
- clippy/fmt/test/workspace-build clean (self-verified). FAIR -t1 vs RSeQC Python: 3.25x (500k).

## rsomics-inner-distance (RSeQC inner_distance) — 2026-05-24 [PARENT RE-VERIFIED] — 7th transcriptomics tool (core RSeQC suite complete)
- BYTE-EXACT freq histogram + per-pair vs RSeQC 5.0.4 (parent golden + 100k synthetic, both diff empty). mRNA-aware inner distance (exon_bases spliced gap via BED12); histogram bins left-excl/right-incl width 5 over -250..250; mapq+proper-pair filter. REPLICATES RSeQC's transcript_ranges bug (1st transcript/chrom silently dropped) for byte-exact compat (match upstream actual behavior incl quirks). coitrees Borrow + exon-bitset dedup.
- FIXED CLI inconsistency: --input-file -> --input (sibling-consistent; -i unchanged). clippy/fmt/test/build clean (self-verified). FAIR -t1 vs RSeQC Python: 2.52x (100k).

## rsomics-featurecounts (subread featureCounts) — 2026-05-24 [PARENT RE-VERIFIED, BUG FIXED] — transcriptomics gene-quant
- Oracle: featureCounts v2.1.1 (conda rs-up, osx-arm64). BYTE-EXACT counts table + .summary on FOUR independent parent fixtures: my 100k-read/200-gene, my 2M-read/500-gene, edge (supplementary read), edge2 (secondary+supplementary+duplicate). All diff-empty (counts gene+all-7-cols, summary all 14 categories).
- ‼️ PARENT CAUGHT REAL BUG (subagent missed): code skipped is_secondary()||is_supplementary() → routed to Unassigned_Secondary. featureCounts DEFAULT *counts* secondary(256)/supplementary(2048)/duplicate(1024); only --primary/--ignoreDup exclude them. Confirmed black-box: 4 reads {flag0,256,2048,1024} over a gene → oracle Assigned=4. Fixed: only filter unmapped + NH>1(MultiMapping) + mapq<Q. Subagent's own adversarial fixture lacked these flags → false-green; subagent flagged it as guess#1.
- SE default (unstranded, -t exon -g gene_id, meta-feature by gene_id, NH>1→MultiMapping, ambiguity→multi-gene overlap). NOT YET: -s strand enforce, PE -p, -O multi-overlap, fractional, --primary/--ignoreDup. CIGAR ref-span (M/D/=/X extend, N splits) replaces seq.len() bug; coitrees interval tree (Borrow normalize) replaces O(N²).
- PERF (representative, wall≫noise): 2M reads/500 genes/201MB BAM, hyperfine -N 8 runs, mac mini_m2 single-thread: ours 1.208s±0.018 vs featureCounts -T1 1.514s±0.022 = **1.25×±0.03 faster**. (100k: 1.27×.) Allocation-free GeneHit enum for single-gene hot path. clippy/fmt/debug-test clean (parent-run).

## rsomics-rnaseq-metrics (Picard CollectRnaSeqMetrics) — 2026-05-24 [PARENT RE-VERIFIED, 2 BLOCKS FIXED] — transcriptomics RNA-seq QC
- Oracle: Picard 3.4.0 (conda rs-up-x64, osx-64/Rosetta). Picard MIT → readable-source port (not clean-room). FIELD-EXACT all 27 metrics (+SAMPLE/LIBRARY/READ_GROUP = 30 cols) vs Picard on parent's INDEPENDENT 20k-read/3-gene fixture across ALL THREE strand modes: NONE, FIRST_READ_TRANSCRIPTION_STRAND, SECOND_READ_TRANSCRIPTION_STRAND (27/27 each). Plus crate golden (no_rrna + with_rrna).
- ‼️ PARENT CAUGHT 2 WRONG BLOCKS the subagent shipped as "documented divergences" (on parent fixture they had non-zero Picard values, exposing them as real bugs): (a) strand-read NUM_R1/UNEXPLAINED — SE reads (no 0x1) are R1 not UNEXPLAINED, population=single-overlapping-gene exon reads, PCT denom excludes UNEXPLAINED (Picard 14663/0; was 0/17001). (b) bias block — per-BASE coverage arrays not 100-sampled bins, rRNA filtered in bias pass, Picard addCoverageCounts skips last base of each block (len-1), stddev=sqrt(E[x²]-mean²) (Picard CV 1.134793/5'0.000031/3'0; was 0.99511/1/1). Base-region block was already exact.
- PERF single-thread vs Picard JVM: 21.4× @100k reads (454ms vs 9.75s, subagent) ; ~137× @20k (61ms vs 8.37s, parent) — Picard wall is JVM+IO dominated, margin enormous & direction unambiguous. clippy/fmt/debug-test clean (parent-run).

## rsomics-clipping-profile (RSeQC clipping_profile.py) — 2026-05-24 [PARENT RE-VERIFIED] — transcriptomics QC, GPL clean-room
- Oracle: clipping_profile.py RSeQC 5.0.4 (pip). BYTE-EXACT .clipping_profile.xls (Position/Clipped_nt/Non_clipped_nt) vs oracle on parent's INDEPENDENT 60k-read fixture (50% reverse-strand → exercises RSeQC's reverse-strand clip MIRRORING pos i→len-1-i; 5% mapq<30 filtered → 57066 used). diff empty.
- Clean-room (RSeQC GPL-v3): method + CIGAR-S spec + black-box. Quirks replicated: read_length=first-passing-read len; secondary/supplementary NOT filtered (only unmapped/qcfail/mapq<Q); zero printed bare "0", nonzero printed "N.0".
- PERF single-thread vs Python: 37.1x @60k (9.5ms vs 352ms, mac mini_m2). clippy/fmt/debug-test clean (parent-run). NOTE: clipping agent created a stub rsomics-tin Cargo.toml to unblock its build (tin agent concurrent) — tin agent overwrote with real impl; verify tin separately.
