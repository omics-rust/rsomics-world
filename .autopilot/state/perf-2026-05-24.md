## ampliconstats-st-reverify — 2026-05-23T16:04:45Z

- ours: `rsomics-bam-ampliconstats -t 1 /Volumes/KIOXIA/Documents/rsomics-world/crates/tools/formats/rsomics-bam-ampliconstats/tests/fixtures/primers_large.bed FIX <fixture>` @ 90fe43c9+dirty
- upstream: `samtools ampliconstats /Volumes/KIOXIA/Documents/rsomics-world/crates/tools/formats/rsomics-bam-ampliconstats/tests/fixtures/primers_large.bed FIX <fixture>` — version: samtools 1.23.1
- machine: Darwin arm64 | Apple M2 | 8 cores
- fixture: crates/tools/formats/rsomics-bam-ampliconstats/tests/fixtures/amplicons_large.bam — 131080101 bytes — sha256 022f490f2e9a86bed234706efe089a540386d61b84bd357296b99c2651245f88
- hyperfine: warmup 3, min-runs 10, shell=none

| side | mean (s) | σ | min | max |
|---|---|---|---|---|
| ours | 0.5873 | 0.0160 | 0.5738 | 0.6267 |
| upstream | 0.6295 | 0.0123 | 0.6113 | 0.6529 |

**ratio (upstream/ours): 1.0718× → PASS** (contract: strictly > 1.0×)


## vcf-fixref-flip-t1 — 2026-05-23T16:30:16Z

- ours: `rsomics-vcf-fixref <vcf> -f <ref.fa> -m flip -o OUT` @ eb5e6fee+dirty v0.1.0
- upstream: `bcftools +fixref <vcf> -o OUT -- -f <ref.fa> -m flip` — version: bcftools 1.23.1
- machine: Darwin arm64 | Apple M2 | 8 cores
- fixture VCF: crates/tools/formats/rsomics-vcf-fixref/tests/fixtures/fixref_bench.vcf — 36217365 bytes — sha256 f46c4da9b2311c3cd6593970b1bf5fc5acf7ac505666b16d7b3bb4ec0822f99b
- fixture FASTA: crates/tools/formats/rsomics-vcf-fixref/tests/fixtures/fixref_bench.fa (244M, 24 chroms × 10Mbp ACGT periodic)
- hyperfine: warmup=3, min-runs=10, shell=sh (cache-warm run after 3 warmups)
- note: variance in cold-cache run is high (0.78–3.1s range); extended run with warm FS cache used

| side | mean (s) | σ | min | max |
|---|---|---|---|---|
| ours | 0.7893 | 0.0188 | 0.7687 | 0.8168 |
| upstream | 3.105 | 0.089 | 3.028 | 3.292 |

**ratio (upstream/ours): 3.93× → PASS** (contract: strictly > 1.0×)

Hot-path analysis: bcftools system time = 2.47s vs ours = 0.33s — bcftools uses htslib's
`faidx_fetch_seq()` which malloc+memcpy-allocs a string per site; our tool does a
single-byte seek+read_exact per site with no heap allocation.


## vcf-fixref-flip-t1-with-fixref-annotation — 2026-05-24

Context: re-measured after adding byte-exact `FIXREF=<action>` INFO annotation (the false-green
compat fix). The annotation adds a `format!` + string replace per record.

- ours: `rsomics-vcf-fixref <vcf> -f <ref.fa> -m flip -o OUT` @ eb5e6fee+dirty v0.1.0
- upstream: `bcftools +fixref <vcf> -o OUT -- -f <ref.fa> -m flip` — version: bcftools 1.23.1
- machine: Darwin arm64 | Apple M2 (mini_m2), macOS 26.5
- fixture VCF: tests/fixtures/fixref_bench.vcf — 36 MB, 500 028 records, sha256 f46c4da9b2311c3cd6593970b1bf5fc5acf7ac505666b16d7b3bb4ec0822f99b
- fixture FASTA: tests/fixtures/fixref_bench.fa — 244 MB
- hyperfine: warmup 2, runs 5, shell=sh

| side | mean (s) | σ | min | max |
|---|---|---|---|---|
| ours | 0.985 | 0.027 | 0.960 | 1.027 |
| upstream | 3.094 | 0.025 | 3.069 | 3.133 |

**ratio (upstream/ours): 3.14× → PASS** (contract: strictly > 1.0×)

Delta vs pre-annotation: 0.789 s → 0.985 s (+0.196 s, +25%). Annotation adds one `format!`
call and one string rebuild per record; the 25% overhead on 500k records is expected and
acceptable — still 3.14× ahead of bcftools.

## fixref-st-reverify — 2026-05-23T16:50:52Z

- ours: `rsomics-vcf-fixref -t 1 -f /Volumes/KIOXIA/Documents/rsomics-world/crates/tools/formats/rsomics-vcf-fixref/tests/fixtures/fixref_bench.fa -m flip FIX <fixture>` @ eb5e6fee+dirty
- upstream: `bcftools +fixref FIX -- -f /Volumes/KIOXIA/Documents/rsomics-world/crates/tools/formats/rsomics-vcf-fixref/tests/fixtures/fixref_bench.fa -m flip <fixture>` — version: bcftools 1.23.1
- machine: Darwin arm64 | Apple M2 | 8 cores
- fixture: crates/tools/formats/rsomics-vcf-fixref/tests/fixtures/fixref_bench.vcf — 36217365 bytes — sha256 f46c4da9b2311c3cd6593970b1bf5fc5acf7ac505666b16d7b3bb4ec0822f99b
- hyperfine: warmup 3, min-runs 10, shell=none

| side | mean (s) | σ | min | max |
|---|---|---|---|---|
| ours | 48.8326 | 1.5596 | 46.7662 | 52.1393 |
| upstream | 3.0733 | 0.0227 | 3.0355 | 3.1075 |

**ratio (upstream/ours): 0.0629× → FAIL** (contract: strictly > 1.0×)

## fixref-st-hashcache — 2026-05-23T17:19:02Z

- ours: `rsomics-vcf-fixref -f /Volumes/KIOXIA/Documents/rsomics-world/crates/tools/formats/rsomics-vcf-fixref/tests/fixtures/fixref_bench.fa -m flip FIX <fixture>` @ eb5e6fee+dirty
- upstream: `bcftools +fixref FIX -- -f /Volumes/KIOXIA/Documents/rsomics-world/crates/tools/formats/rsomics-vcf-fixref/tests/fixtures/fixref_bench.fa -m flip <fixture>` — version: bcftools 1.23.1
- machine: Darwin arm64 | Apple M2 | 8 cores
- fixture: crates/tools/formats/rsomics-vcf-fixref/tests/fixtures/fixref_bench.vcf — 36217365 bytes — sha256 f46c4da9b2311c3cd6593970b1bf5fc5acf7ac505666b16d7b3bb4ec0822f99b
- hyperfine: warmup 3, min-runs 10, shell=none

| side | mean (s) | σ | min | max |
|---|---|---|---|---|
| ours | 0.9009 | 0.0207 | 0.8777 | 0.9419 |
| upstream | 3.1115 | 0.0280 | 3.0691 | 3.1629 |

**ratio (upstream/ours): 3.4539× → PASS** (contract: strictly > 1.0×)


## bam-signal-st — 2026-05-24T01:45:00Z

- ours: `rsomics-bam-signal -t 1 -o OUT FIX` @ 7a306224
- upstream: `bamCoverage -b FIX -o OUT.bedgraph --outFileFormat bedgraph --binSize 50 -p 1` — version: bamCoverage 3.5.6
- machine: Darwin arm64 | Apple M2 | 8 cores
- fixture: bench.bam (markdup_1m_cs synthetic WGS, coord-sorted, ~1M reads) — 62369993 bytes — sha256 e202f792df45409d2e6498ff80319407d09a32f6fe6a0c4431d2ffdb563e63b6
- hyperfine: warmup 2, min-runs 7, default shell

| side | mean (s) | σ | min | max |
|---|---|---|---|---|
| ours | 0.2567 | 0.0051 | 0.2499 | 0.2681 |
| upstream | 3.9862 | 0.0436 | 3.9433 | 4.0672 |

**ratio (upstream/ours): 15.5312× → PASS** (contract: strictly > 1.0×)

Note: deeptools is single-process Python with pysam/numpy overhead. Our Rust implementation uses rsomics-bamio raw-record path with libdeflate BGZF, flat Vec<u32> bin arrays, no per-record allocation, and a single scan pass. All bins (including zero) emitted; adjacent equal-value bins merged. Output byte-exact vs deeptools 3.5.6 on 62MB fixture.


## rsomics-bam-signal (deeptools bamCoverage) — 2026-05-24
- ours `-t 1 <bam> -o OUT` vs `bamCoverage -b <bam> -o OUT --outFileFormat bedgraph --binSize 50 -p 1`
- **-t1 fair (thread-matched, warm hyperfine -r7): 14.86 ± 0.81x faster** (ours ~0.27s vs deeptools ~4.0s)
- upstream: deeptools bamCoverage 3.5.6 (pip, macOS arm64 Apple M2)
- fixture: tests/fixtures/bench.bam — 62MB coord-sorted, ~1M reads (gitignored)
- compat: BYTE-EXACT bedGraph vs deeptools 3.5.6 (golden binSize 50+100 + 62MB bench)
- NOTE: scripts/perfgate.sh can't be used here — it symlinks only the BAM, dropping the .bai that deeptools (and indexed readers) require; measured via direct hyperfine instead. (perfgate .bai-pair handling = a harness follow-up.)


## vcf-setgt-t-q — 2026-05-24

- ours: `rsomics-vcf-setgt --target q -i 'FMT/DP<5' -n . -o OUT` @ rsomics-vcf-setgt v0.2.0
- upstream: `bcftools +setGT -o OUT -- -t q -i 'FMT/DP<5' -n .` — version: bcftools 1.23.1
- machine: Darwin arm64 | Apple M2 | 8 cores (mini_m2)
- fixture: /tmp/setgt-bench.vcf — 93.9 MB — 100,000 records × 100 samples — GT:DP:GQ (seed 42, random.randint)
- hyperfine: warmup 1, runs 5

| side | mean (s) | σ |
|---|---|---|
| ours (-t q) | 0.776 | 0.090 |
| bcftools (-t q) | 0.936 | 0.028 |

**ratio (upstream/ours): 1.21× → PASS** (contract: strictly > 1.0×)

### Simple mode (-t a -n .) — no expression eval

| side | mean (s) | σ |
|---|---|---|
| ours (-t a) | 0.362 | 0.043 |
| bcftools (-t a) | 0.799 | 0.030 |

**ratio (upstream/ours): 2.21× → PASS**


## rsomics-fragment-size (PE insert-size distribution) — 2026-05-24
- correctness AUTHORITY: VALUE-EXACT vs `samtools stats -m 1.0` IS-lines (0 differing sizes on golden 2000-pair + 1.5M-pair bench; counts paired+proper-pair+mapped+read1 |TLEN|, matching samtools' inward-FR-pair rule; samtools default -m 0.99 trims top 1%, -m 1.0 = no-trim match). cross-check: value-exact vs picard CollectInsertSizeMetrics histogram too.
- perfgate (fair same-op, full-scan): ours -t1 vs `picard CollectInsertSizeMetrics` on 62MB/1.2M-pair PE BAM (mac M2): subagent hyperfine 289.6ms vs 9.616s = 33.2x; parent re-verify ~0.27s vs 10.5s ≈ 39x. Overwhelming (picard = JVM+R full-scan, genuine compute). PASS.
- (samtools stats -m 1.0 = 2.74s but does MANY stats beyond IS → not the fair same-op oracle; picard is.)

## rsomics-methyldackel (MethylDackel extract) — 2026-05-24
- BYTE-EXACT vs real MethylDackel 0.6.1 (BUILT FROM SOURCE: htslib brew + libBigWig -DNOCURL) — golden authentic (not circular), data lines identical on golden + 74M output on calmd_30x.bam (2.5M CpG lines, diff=0).
- -t1 perfgate: ours vs real MethylDackel extract single-thread (calmd_30x.bam 92MB/4M reads, mac M2, hyperfine 6 runs): 1.59x (re-verified; subagent fix reported 1.58x). PASS.
- FIX history: 1st build claimed 3.8x but real -t1 was 0.66x (6th overclaim; allocation/page-fault storm — buffered ALL reads → 2.2GB RSS, 3.09s system). Fixed via streaming overlap-window (settle reads when ref-end passes cursor, scatter CpG calls, drop; split-borrow mate-pair, zero per-pair clones): RSS 2.2GB→116MB, system 3.09s→0.10s, -t1 0.66x→1.59x.

## rsomics-bam-fingerprint (deeptools plotFingerprint) — 2026-05-24
- BYTE-EXACT vs deeptools plotFingerprint 3.5.6 --outRawCounts: 216/216 config combos (4 BAMs × 9 bin-sizes × 6 sample-counts) + the real 62MB BAM at full defaults (498540 bins). Matched deterministic stepSize sampling + both deeptools code paths (stepSize!=binSize single-bin clamp vs stepSize==binSize tiled over-count quirk) + CIGAR get_blocks (M/=/X split by I/D/N) + skip-unmapped filter.
- perfgate -t1 vs plotFingerprint -p1 (62MB/1.2M-read indexed BAM, mac M2): DEFAULT (bs500 n500000) ours 0.52s vs deeptools ~1357s (deeptools default = 500k separate index fetch() calls, pathological); matched n1000 = 8.26x (subagent). Win overwhelming at every matched config. PASS.
- scoped: PNG/SVG plot (we emit the data table, like bam-signal→bedGraph), --JSDsample JS-distance/CHANCE/Synthetic cols.

## rsomics-coverage-core (Layer-A) + rsomics-bam-compare (deeptools bamCompare) — 2026-05-24
- REFACTOR GUARD: bam-signal (published 0.1.0→0.1.1) migrated to consume the new rsomics-coverage-core Layer-A binned-coverage primitive — STILL BYTE-EXACT vs deeptools bamCoverage (compat + 62MB/180449-line check, 278ms -t1, no regression). Extraction behavior-preserving. no-cp honored.
- bam-compare: BYTE-EXACT vs deeptools bamCompare 3.5.6 — all 8 ops (log2/ratio/reciprocal_ratio/subtract/add/mean/first/second) + binSize 50/100 + pseudocount, golden + 62MB self-compare (log2) + subagent's 62MB+115MB/197853-line pair. Default readCount scaling (min(m1,m2)/mi) + log2 + pseudocount[1,1] per bamCompare.py.
- perfgate -t1 vs bamCompare -p1: re-verified 32.26x (62MB self-compare bs50, hyperfine); subagent 15.54x (62MB+115MB pair). PASS overwhelmingly (deeptools single-thread Python).
- scoped: SES scaling, --normalizeUsing, extend/center, smoothLength, region/blacklist, bigWig.

## rsomics-multibam-summary (deeptools multiBamSummary) — 2026-05-24
- Built on rsomics-coverage-core 0.1.0 (compute_coverage per BAM -> assembled matrix). 3rd consumer of the Layer-A lib.
- VALUE-EXACT vs deeptools multiBamSummary 3.5.6 --outRawCounts: bins mode (10000 bins, big_a+big_b ~chr1 fixtures, header + ALL rows byte-identical) AND BED-file mode (500 regions). -t1 output identical to default-thread.
- perfgate FAIR -t1: ours -t1 473.6ms +/-3.6 vs deeptools bins -p1 9.600s +/-0.098 = 20.27x +/-0.26 (hyperfine r4 warmup1, Mac14,3 arm64). NOTE default-thread ours = 67.9x but that is multi-vs-single (unfair); 20.27x is the honest thread-for-thread number. PASS >1.0x.
- modes: bins + BED-file feature-complete; numpy .npz output scoped (--outRawCounts is the value oracle). re-verified by parent (killed subagent's claims not trusted).

## compute-matrix — 2026-05-24

deeptools `computeMatrix` port (bigWig signal → per-region score matrix). Own
pure-Rust bigWig reader (`src/bigwig.rs`, flate2 zlib-rs inflation) — bigtools
NOT adopted: it pins `libdeflater 0.13` whose C-FFI `libdeflate-sys`
(`links="libdeflate"`) collides at link time with the workspace's
`rsomics-fqgz` (`libdeflater 1.x`).

- ours: `rsomics-compute-matrix {reference-point|scale-regions} -S bench.bw -R bench.bed -o OUT.gz ... -t 1 -q` @ v0.1.0
- upstream: `computeMatrix {reference-point|scale-regions} -S bench.bw -R bench.bed -o OUT.gz ... -p 1 -q` — version: deeptools 3.5.6 (miniforge)
- machine: Darwin arm64 | Apple M2 (mini_m2) | 8 cores, macOS 26.5
- fixture bigWig: tests/fixtures/bench.bw — 258810 bytes — sha256 b2f010877e34884a746ddc862c1d60010b1d8e811a179702cd4f6e298d3d64ea (3 chroms, peaky 25-bp bigWig from synthetic 86k-read BAM)
- fixture BED: tests/fixtures/bench.bed — 2100 BED6 regions, mixed strand — sha256 ce062be10864f39aa18304c06671fd0370ea545d517876e2604a5383380f9918
- hyperfine: warmup 1, min-runs 5

VALUE-EXACT vs deeptools 3.5.6: ours -t1 data rows byte-identical to deeptools -p1
on the 2100-region fixture (reference-point TSS/TES/center + scale-regions), and
header JSON byte-identical save `proc number` (a runtime knob). Both single-thread.

reference-point (TSS, b2000 a2000 bs50):

| side | mean (s) | σ | min | max |
|---|---|---|---|---|
| ours -t1 | 0.3351 | 0.0032 | 0.3303 | 0.3400 |
| deeptools -p1 | 3.416 | 0.041 | 3.368 | 3.470 |

**ratio (upstream/ours): 10.19× → PASS** (contract: strictly > 1.0×)

scale-regions (m2000 b1000 a1000 bs50):

| side | mean (s) | σ | min | max |
|---|---|---|---|---|
| ours -t1 | 0.4728 | 0.0047 | 0.4661 | 0.4789 |
| deeptools -p1 | 3.309 | 0.035 | 3.270 | 3.360 |

**ratio (upstream/ours): 7.00× → PASS**

Peak RSS (/usr/bin/time -l, reference-point): ours 5.5 MB vs deeptools 70.2 MB
(~12.7× lower). deeptools loads per-base into numpy float arrays; ours streams
per-flank.

modes: reference-point feature-complete (TSS/TES/center, both strands, asym
flanks, off-chrom NaN pad, all 6 bin stats, missing-data-as-zero, scale, min/max
threshold, skip-zeros); scale-regions feature-complete (scaled body + flanks,
both strands, short-body all-NaN case). Scoped: multi-sample/multi-group,
#-grouped BED, GTF, unscaled 5'/3', nanAfterEnd, blacklist, sortRegions!=keep.

A real bug (numpy linspace float-rounding at bin boundaries: must compute
`step=total/nbins` then `idx*step`, not `idx/nbins*total`) was caught only by the
large fixture — small fixture was a false-green.

## rsomics-compute-matrix (deeptools computeMatrix) — 2026-05-24 [PARENT RE-VERIFIED]
- Self-built pure-Rust BBI/bigWig reader (src/bigwig.rs, flate2 zlib-rs). bigtools UNUSABLE: its libdeflater 0.13 (links="libdeflate") collides with workspace rsomics-fqgz/fastq-* libdeflater 1.x (single-links rule = cargo hard error). Quadrant (1) pure-Rust. format from bigtools source (MIT) + Kent BBI spec.
- VALUE+HEADER EXACT vs deeptools 3.5.6 (parent-verified LIVE on 2100-region bench fixture, both modes): reference-point (TSS/TES/center) AND scale-regions — @-JSON header + ALL matrix rows byte-identical (excl runtime "proc number"). -t1 output identical to default.
- FAIR perfgate -t1 (parent re-verified; ours -t1 User~=wall = genuine single-thread): reference-point ours 340.6ms vs deeptools -p1 3.424s = 10.05x +/-0.27; scale-regions ours 469.6ms vs 3.295s = 7.02x +/-0.06. PASS. subagent reported 10.19x/7.00x — matches. Peak RSS ~12.7x lower.
- bug the small fixture hid (caught by subagent on large): numpy linspace bin partition = step=total/nbins then idx*step (float-rounding at boundaries). modes feature-complete; multi-sample/group, GTF, blacklist, clustering scoped.
