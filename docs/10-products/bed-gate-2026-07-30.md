# `rsomics-bed` representative product gate — 2026-07-30–31

Status: representative Linux `x86_64` evidence complete; exact-head
four-native-target CI green; `rsomics-bed 0.1.0` published and independently
verified.

## Scope

This gate evaluates the five implemented operations: `sort`, `merge`,
`intersect`, `subtract`, and `complement`. It does not add another operation,
revive an operation-sized repository, publish a crate, or invent a new public
foundation.

## Audit correction

The inherited 50,000-record benchmark used non-overlapping A and B records for
intersect and subtract. Both commands produced empty output, so the recorded
72.20x and 68.43x figures measured a sparse no-hit case rather than a
representative hot path.

The sparse case remains a diagnostic, but is no longer a release claim. The
product benchmark now uses:

- ten chromosomes;
- groups of five overlapping intervals for merge;
- one hit for every A record in intersect;
- a duplicate B interval for every fiftieth record;
- two emitted fragments per A record in subtract;
- actual complement gaps.

## Exact identities

- product implementation:
  `ed415eeebd9d6a3bcb34cc9cf15bcfc5f7c587cd`;
- product evidence head:
  `76d02dbc9c0fd549782f1e68e2b0ef5e64f13d45`;
- product documentation head:
  `97f5fe31662eb66aa7fad42dc4f62f3007783280`;
- shared-contract alignment head:
  `478673db5e3095880ed5de7f8c1cb63436d93c50`;
- checked-boundary refactor head:
  `9f4ba8ee945c487b4157bf38eba7a6577fca5dfd`;
- product-private overlap-index refactor:
  `0303db3214e6ad748744a076c109f531bd1cd4cc`;
- registry-foundation migration:
  `e8898dbcb0db7a398fbd84f7627ad2be322f475f`;
- performance documentation:
  `e460984c387206a2e32bb04dc4cc9dbf504ac10b`;
- published source head:
  `c252c6036b4173fc95209938b45fc032ca34872d`;
- post-release shared transactional-output head:
  `989894f2dad5c669b08fddc1c15b938c237e5303`;
- evidence-head CI run `30556922040` and documentation-head CI run
  `30557379937`, both green on native Linux and macOS for `x86_64` and
  `aarch64`;
- alignment-head CI run `30569761393` and boundary-head CI run `30570159631`,
  green on the same four native target classes;
- published-source CI run `30598480894`, green on the same four native target
  classes;
- publish run `30598715133`;
- transactional-output exact-head CI run `30621067404`, green on the same
  four native target classes with the pinned bedtools 2.31.1 oracle;
- post-refactor Linux release binary SHA-256:
  `e78a4d67091ffa121d12d451c0e10526ca7effccbebd73afb7ba7ffcddbc009f`;
- `rustc 1.91.0 (f8297e351 2025-10-28)`;
- bedtools `v2.31.1`, release archive SHA-256
  `fc7e660c2279b1e008b80aca0165a4a157daf4994d08a533ee925d73ce732b97`;
- bedtools binary SHA-256:
  `40c465f999a58dc8ff42959ff8635ede4f54dce51a4309e0fa43afa7c9a38e56`;
- current main-branch foundations: `rsomics-common 0.8.0`, `rsomics-help
  0.4.0`, and `rsomics-intervals 0.3.0`. The published 0.1.0 archive remains
  tied to its recorded common 0.7 release head.

## Correctness

The current targeted suite passes 40 library tests, 12 CLI tests, and three
compatibility tests against the real bedtools 2.31.1 binary in both debug and
release profiles.

The control-plane differential runner additionally varied ordering, overlap
density, duplicate intervals, zero-length intervals, chromosomes, and trailing
BED columns. It passed:

- 500 deterministic trials on macOS `arm64`;
- 1,000 deterministic trials on Linux `x86_64`;
- seed `200090385`;
- all five operations per trial.

For sort, bedtools may reorder exact coordinate ties while rsomics deliberately
preserves their input order. The differential therefore requires identical
coordinate ordering and record multiplicity for ties. Other representable
cases require byte-identical output. The already documented origin-zero
fail-loud divergence remains unchanged.

The one-million-record representative outputs matched bedtools byte for byte:

| Operation | Output lines | Output bytes | SHA-256 |
|---|---:|---:|---|
| sort | 1,000,000 | 30,766,670 | `ff81f1cb965c57d8dfe282a76382e54579b5fdf7f24929837a71220ac1a8c209` |
| merge | 200,000 | 4,177,770 | `c26e988fae5a0ba8e6835117609ee65d2983d5b32ee24f8b9396e65d20d1b038` |
| intersect | 1,020,000 | 31,381,970 | `40a55d578b0b58e58c3c124e39850d31ac9a551c5a4f77a491891707e37c94b6` |
| subtract | 2,000,000 | 61,533,350 | `6bbd97179ccfbd6a6d545b77f3c56cdea1a7661ad60b4dbcbef01a233c361772` |
| complement | 200,010 | 4,177,940 | `f66397a8bdd2109271c667715f0e7a18a82f7cfe99618475c883216b936964fc` |

After the overlap-index refactor, another 200 seeded differential trials
passed all five operations on the same pinned Linux oracle.

## Performance decision

The host was `dell-Precision-7920-Tower`, Ubuntu 22.04, Linux
6.8.0-90-generic, `x86_64`, with two Intel Xeon Gold 6238R CPUs. Commands were
bound to physical cores 48-51. GNU `time` ran one warmup followed by ten
measurements. Times are means and sample standard deviations; RSS is the
median process high-water mark in KiB.

| Operation | rsomics | bedtools | Speedup | RSS, ours / bedtools | Decision |
|---|---:|---:|---:|---:|---|
| sort | 0.390 ± 0.000 s | 1.077 ± 0.016 s | 2.76x | 197,120 / 360,624 | throughput and memory pass |
| merge | 0.197 ± 0.007 s | 0.228 ± 0.004 s | 1.16x | 1,792 / 4,480 | throughput and memory pass |
| intersect | 0.672 ± 0.004 s | 2.683 ± 0.116 s | 3.99x | 114,700 / 344,960 | throughput and memory pass |
| subtract | 0.592 ± 0.009 s | 3.011 ± 0.023 s | 5.09x | 18,816 / 344,960 | throughput and memory pass |
| complement | 0.236 ± 0.007 s | 0.302 ± 0.015 s | 1.28x | 4,480 / 4,480 | throughput and memory pass |

All five operations pass the representative throughput gate. The 1.16x merge
margin is real on this host and distribution, but narrow enough that future
releases must remeasure rather than inherit the claim.

These measurements use the product-private overlap index and subtract coverage
map that shipped in `0.1.0`.

## Foundation and CLI findings

`intersect` uses a product-private COITrees index because ordering,
multiplicity, zero-length behavior, and coordinate-boundary policy are BED
contracts rather than generic interval contracts. `subtract` uses a separate
product-private merged `u64` coverage map because it does not need an overlap
tree. The shared `rsomics-intervals` surface is limited to the coordinate model
used by BED and annotation.

The shared CLI migration removes threads, seed, quiet, and verbose controls
from the BED command tree. The remaining global option is the demonstrated
JSON output contract. The post-release main branch removes its 71-line private
named-output transaction and consumes `rsomics-common::write_atomic`; its
destination-mode and failure-preservation tests continue to pass.

## Raw evidence

Remote directory:
`/data1/liangjy/rsomics-linux-x86_64-20260731/bed-gate`.

Representative JSON:
`/data1/liangjy/rsomics-linux-x86_64-20260731/bed-gate/results/representative-1m.json`,
SHA-256
`2133285863696e1b22f10aadbd69e5f80e16af235d8a2ba640cfb1d567070a46`.

The product's post-change local gates passed formatting, strict Clippy, debug
and release tests, benchmark smoke, and package verification with all Cargo,
target, and temporary paths on KIOXIA. Exact-head CI repeated formatting,
strict Clippy, debug and release tests, the live pinned oracle, and package
verification across the four native target classes.

The crates.io API reports archive checksum
`9c8883539cb78d946bdb1355704e1b8810130a2b9eadab8c37e94bf3c25f4ebe`.
The independently downloaded archive matched that checksum and passed 40
library tests, 12 CLI tests, three live-oracle compatibility tests, and a CLI
sorting smoke test.

## Follow-up

Native Linux `aarch64` remains covered by correctness CI rather than a
representative performance host. Future releases must re-run the representative
performance gate instead of inheriting these measurements.
